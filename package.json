{
  "name": "rg-quiz-monorepo",
  "version": "1.0.0",
  "description": "Root package.json for RG Quiz App monorepo",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}